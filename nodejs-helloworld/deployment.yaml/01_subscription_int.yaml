apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: reversewords-int-app-subscription
  namespace: reverse-words-int
  labels:
    app: reversewords-int-app
  annotations:
    apps.open-cluster-management.io/github-path: apps/reversewords/
    apps.open-cluster-management.io/github-branch: int
spec:
  channel: acm-demo-app-channel/acm-demo-app-1
  placement:
    placementRef:
      kind: PlacementRule
      name: integration-clusters